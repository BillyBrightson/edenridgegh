# This directory must never be reachable over HTTP.
# It sits outside both document roots; this file is a second line of defence.
Require all denied
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
